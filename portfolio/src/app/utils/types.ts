export interface Project {
    title: string,
    cardImage?: string,
    mockup?: string,
}

export interface ProjectDetails {
    title: string,
    description: string,
    techstack: string,
    mockup?: string,
    demo?: string,
    imageSm?: string,
    imageLg?: string,
}

